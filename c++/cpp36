#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>

using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c; // Read the three input integers
    int sum = a + b + c; // Calculate their sum
    cout << sum << endl; // Print the sum to stdout
    return 0;
}




